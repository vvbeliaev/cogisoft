PUBLIC_POSTHOG_URL="https://eu.i.posthog.com"
PUBLIC_POSTHOG_TOKEN="pub"

# Path to the SQLite database file inside the container.
# Mount the parent directory as a volume to persist data.
SQLITE_DB_PATH="/data/cogisoft.db"